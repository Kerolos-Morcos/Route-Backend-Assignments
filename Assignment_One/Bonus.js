// Counter II - on LeetCode
/*
function createCounter(init) {
    let currentNum = init;
    return {
        increment: () => currentNum += 1,
        decrement: () => currentNum -= 1,
        reset: () => currentNum = init
    }
}
const result = createCounter(5);
console.log(result.increment());
console.log(result.reset());
console.log(result.decrement());
*/